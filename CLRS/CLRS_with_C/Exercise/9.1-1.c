/*Exercise 9.1-1*/
/*15.8.10*/
#include <stdio.h>
#include <stdlib.h>

struct Node
{
	int key;
	Node *next;
	Node *p;
	Node *left;
	Node *right;
	Node(int k):key(k),next(NULL),p(NULL),left(NULL),right(NULL){}
};

Node* prepareLeaves(const );
int Find_S2(Node *head);

int solve9_1_1()
{

}

int Find_S2(Node *head)
{
	Node *p, *q, *r, *t;
	while (head.next != NULL)
	{
		P = head;
		head = NULL;
		while(p)
		{
			if(p.next == NULL)
			{
				
			}
		}
	}
}