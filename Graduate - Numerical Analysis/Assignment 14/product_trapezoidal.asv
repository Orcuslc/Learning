function res = product_trapezoidal(f, b, n, alpha)
% Product trapezoidal rule;
% Compute integral of `f` on `[0, b]` with order `n`;

phi1 = @(k) trapz(@(u) u./(k-1+u).^alpha, 0, 1);
phi2 = @(k) tra(@(u) (1-u)./(k-1+u).^alpha, 0, 1);

h = b/n;
x = 0:h:b;
w = zeros(n+1, 1); w(1) = phi2(0); w(n+1) = phi1(n-1);
for i = 2:n
    w(i) = phi1(i-2)+phi2(i-1);
end
disp(w)
res = sum(w.*f(x))/(h^alpha);