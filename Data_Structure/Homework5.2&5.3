2.proof:
	事实上，如果P(k) < P(i)成立，那么第i个元素出栈之后，第k个元素出栈之前，要么有元素进栈，要么元素一直出栈；无论哪一种情形，由于第j个元素是在第k个元素之前出栈的，那么P(j) > P(k)必然成立。

3.准则为：在一个序列的前i项中，S的个数不小于X的个数，对于任意的i均成立，且整个序列中两者个数相等。
Proof:
	对于两个不同的序列而言，假设他们前K项输出的序列是相同的，那么对于其K+1项，不妨设A序列为S，B序列为X(事实上，必然存在这样的项，否则两个序列完全相同。)，那么设B序列此时输出的项为a(i)，那么设A序列下一个X的位置为K+j(由合法性知存在)，那么A的下一个输出为a(i+j)，故两个序列的输出不同。