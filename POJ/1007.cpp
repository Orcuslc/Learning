#include <iostream>
#include <string>

using namespace std;

class str_rev {
public:
	string str;
	int rev;
private:
	int count_rev();
};

int str_rev::count_rev() {
	rev = 0;
	
}

int main() {
	int len, num;
	cin >> len >> num;	
	return(0);
}