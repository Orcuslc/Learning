#include <iostream>

using namespace std;
const int MAX = 101;
int brokers[MAX][MAX] = {-1};

int main {
	int nbrokers, n;
	while(cin >> nbrokers) {
		if(brokers == 0) break;
		for(int i = 1; i <= brokers; i++) {

		}
	}
}